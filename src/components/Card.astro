---
const { title, description, class: extraClasses } = Astro.props;
---
<div class:list={["flex flex-col gap-5 grow text-center max-w-xl p-10 text-gray-200 bg-blue-950 rounded-4xl shadow-2xl shadow-black", extraClasses]}>
    <h1 class="font-semibold text-4xl">{title}</h1>
    <hr class="border-white my-2">
    <p class="text-xl text-justify w-full">{description}</p>
</div>